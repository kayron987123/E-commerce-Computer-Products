# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy JAR app to Azure Web App - springbootecommerce

on:
  push:
    branches:
      - gad
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java version
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'microsoft'

      - name: Build with Maven
        run: mvn clean install -DskipTests  # Opción para omitir las pruebas
        env:
          PASSWORD_DB: 'admin123@'
          PASSWORD_EMAIL: 'jmyp yqgf lkvs jyun'
          PAYPAL_CLIENT_ID: 'AdFNkDPGFfLYklRn9xSNv_GtyM-j8IJK1_cnc504FVe5f2l68UnNkPmE7t9ZCjefD6xT0El_6ccKqdBi'
          PAYPAL_CLIENT_SECRET: 'EO8_OVcbQSzKmYrfaaTaBGrNvmDryxa0aRP7GDg-V2PQdaNVgJ9M-_q_4gU_erqjRtzLvKtbt4scHG5H'
          PAYPAL_MODE: 'sandbox'
          REDIS_HOST: 'ecommerce.redis.cache.windows.net'
          REDIS_PASSWORD: 'IlRR5WIdtF2znRa8MquYFRNteuI9lyimsAzCaDuFjSs='
          REDIS_PORT: '6380'
          URL_DB: 'jdbc:mysql://ecommerce-pharmacy.mysql.database.azure.com:3306/ecommerce'
          USER_DB: 'rootadmin123'
          USER_EMAIL: 'josuealva920@gmail.com'

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: java-app
          path: '${{ github.workspace }}/target/*.jar'

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
      
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: java-app
      
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'springbootecommerce'
          slot-name: 'Production'
          package: '*.jar'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_74C4288904874F1A80B8E7712FE54234 }}
